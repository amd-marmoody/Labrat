# fzf configuration (managed by LabRat)
# This file is used via FZF_DEFAULT_OPTS_FILE
# Colors are loaded separately from themes

# Layout
--height=40%
--layout=reverse
--border=rounded
--info=inline

# Behavior
--bind=ctrl-/:toggle-preview
--bind=ctrl-d:half-page-down
--bind=ctrl-u:half-page-up

# Preview options (applied when relevant)
--preview-window=right:50%:wrap

# Markers
--marker='▌'
--pointer='▶'
